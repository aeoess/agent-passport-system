# CLAUDE.md

This file exists so Claude Code picks up repository context. See `AGENTS.md` for the canonical
contributor instructions. Nothing volatile lives here; versions and counts are read from
`package.json` and CI, never from this file.
